import { AutolinkPipePipe } from './autolink-pipe.pipe';

describe('AutolinkPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AutolinkPipePipe();
    expect(pipe).toBeTruthy();
  });
});
