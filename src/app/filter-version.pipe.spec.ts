import { FilterVersionPipe } from './filter-version.pipe';

describe('FilterVersionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVersionPipe();
    expect(pipe).toBeTruthy();
  });
});
